@model CreatePullRequestViewModel

<form asp-controller="Repository" asp-action="CreatePullRequest" method="post">
	<fieldset>
		<legend>Create New Pull Request</legend>
		<input type="hidden" asp-for="@Model.RepositoryId" />
		<div class="form-group">
			<label asp-for="@Model.Title" class="form-label mt-4">Title</label>
			<input asp-for="@Model.Title" class="form-control" placeholder="Enter Title">
			<span asp-validation-for="@Model.Title" class="text-danger"></span>
		</div>
		<div class="form-group">
			<label asp-for="@Model" class="form-label mt-4">Description</label>
			<textarea asp-for="@Model.Description" class="form-control" rows="3" placeholder="Enter Description"></textarea>
			<span asp-validation-for="@Model.Description" class="text-danger"></span>
		</div>
		<br />
		<hr />
		<button type="submit" class="btn btn-primary">Create</button>
		<a asp-controller="Repository" asp-action="DetailsPage" asp-route-id="@Model.RepositoryId" asp-route-tab="pr" class="btn btn-info">Cancel</a>
	</fieldset>
</form>

@section Scripts {
	<partial name="_ValidationScriptsPartial" />
}
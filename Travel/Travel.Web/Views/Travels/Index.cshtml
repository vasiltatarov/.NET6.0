@model TravelsViewModel

<a asp-controller="Travels" asp-action="AddTravel" class="btn btn-primary">Add Travel</a>

<div>
    @foreach (var travel in Model.Travels)
    {
        <h1>Destination: @travel.Destination</h1>
        <h2>Participants: @travel.Participants</h2>
    }
</div>